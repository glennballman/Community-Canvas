Found It! Here's What We Have
Great audit! The "Command Center" does exist - it's at /legacy pointing to Dashboard.tsx. Let me map everything to your 5-area vision:

Current State vs Glenn's Vision
Glenn's AreaWhat Exists NowRouteStatus1. Community ManagerDashboard.tsx with municipality toggle, alerts, weather, ferries/legacy✅ EXISTS but hidden2. Business OwnerFleetDashboard.tsx, Host Dashboard, AccommodationsNo fleet route!⚠️ PARTIAL3. Crew/TravellerTripPlanning components, Participant formsNo dedicated routes⚠️ ORPHANED4. System AdminAdminLayout with all admin pages/admin/*✅ WORKING5. Consumer/PublicStaging search, map, property details/staging/*✅ WORKING

PROMPT: Proper Navigation Restructure
I need to restructure navigation WITHOUT breaking existing functionality. The current Dashboard.tsx at /legacy is our Command Center - do NOT remove it.

## Step 1: Understand Current Route Structure

The key discovery:
- `/legacy` → Dashboard.tsx (THIS IS THE COMMAND CENTER with municipality toggle)
- `/` → PublicDashboard (consumer landing page)
- FleetDashboard component EXISTS at client/src/components/Fleet/FleetDashboard.tsx but has NO ROUTE

## Step 2: Create New Route Structure

Update client/src/App.tsx to add these NEW routes (keep all existing routes):
```tsx
// Add these imports at the top
import FleetDashboard from './components/Fleet/FleetDashboard';

// In the Routes section, ADD these new routes (don't remove existing):

{/* COMMAND CENTER - Rename /legacy to /command-center but keep /legacy as alias */}
<Route path="/command-center" element={<Dashboard />} />
<Route path="/legacy" element={<Dashboard />} />

{/* FLEET/BUSINESS OWNER ROUTES */}
<Route path="/fleet" element={<FleetDashboard />} />
<Route path="/fleet/vehicles" element={<FleetDashboard />} />
<Route path="/fleet/trailers" element={<FleetDashboard />} />
<Route path="/fleet/drivers" element={<FleetDashboard />} />

{/* CREW/TRAVELLER ROUTES - Create placeholder pages */}
<Route path="/crew" element={<CrewDashboard />} />
<Route path="/crew/trips" element={<CrewTrips />} />
<Route path="/crew/check-in" element={<CrewCheckIn />} />
```

## Step 3: Create Main Navigation Component

Create client/src/components/MainNav.tsx:
```tsx
import React from 'react';
import { Link, useLocation } from 'react-router-dom';

interface NavItem {
  path: string;
  label: string;
  icon: string;
  description?: string;
}

interface NavSection {
  title: string;
  items: NavItem[];
  color: string;
}

const navSections: NavSection[] = [
  {
    title: 'Community Manager',
    color: 'blue',
    items: [
      { path: '/command-center', label: 'Command Center', icon: '🛡️', description: 'Real-time emergency & infrastructure monitoring' },
      { path: '/admin/civos', label: 'CivOS Operations', icon: '📡', description: 'Emergency signals & capacity' },
      { path: '/admin/geo', label: 'Geographic Data', icon: '🗺️', description: 'Municipalities & regions' },
      { path: '/admin/infrastructure', label: 'Infrastructure', icon: '🏗️', description: 'Roads, bridges, utilities' },
    ]
  },
  {
    title: 'Business Owner',
    color: 'green',
    items: [
      { path: '/host/dashboard', label: 'Property Dashboard', icon: '🏨', description: 'Manage your accommodations' },
      { path: '/fleet', label: 'Vehicle Fleet', icon: '🚛', description: 'Manage vehicles & trailers' },
      { path: '/admin/accommodations', label: 'All Accommodations', icon: '🏠', description: '7,458 properties' },
    ]
  },
  {
    title: 'Crew & Traveller',
    color: 'yellow',
    items: [
      { path: '/staging', label: 'Find Staging', icon: '🔍', description: 'Search crew-friendly stays' },
      { path: '/staging/map', label: 'Map View', icon: '📍', description: 'Visual property search' },
      { path: '/staging/bookings', label: 'My Bookings', icon: '📅', description: 'Upcoming stays' },
      { path: '/staging/chamber', label: 'Local Services', icon: '🏛️', description: 'Chamber of Commerce' },
    ]
  },
  {
    title: 'System Admin',
    color: 'purple',
    items: [
      { path: '/admin', label: 'Admin Console', icon: '⚙️', description: 'System overview' },
      { path: '/admin/import', label: 'Data Import', icon: '📥', description: 'Import properties & data' },
      { path: '/admin/sources', label: 'Data Sources', icon: '🔗', description: 'Manage feeds' },
      { path: '/admin/settings', label: 'Settings', icon: '🔧', description: 'System configuration' },
    ]
  },
];

export default function MainNav() {
  const location = useLocation();
  
  const colorClasses: Record<string, string> = {
    blue: 'border-blue-500 bg-blue-500/10',
    green: 'border-green-500 bg-green-500/10',
    yellow: 'border-yellow-500 bg-yellow-500/10',
    purple: 'border-purple-500 bg-purple-500/10',
  };

  return (
    <div className="p-6 space-y-8">
      <div className="text-center mb-8">
        <h1 className="text-3xl font-bold text-white mb-2">🍁 Community Canvas</h1>
        <p className="text-gray-400">BC Staging Network - Navigation Hub</p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {navSections.map(section => (
          <div 
            key={section.title}
            className={`border-l-4 ${colorClasses[section.color]} rounded-lg p-4`}
          >
            <h2 className="text-lg font-semibold text-white mb-3">{section.title}</h2>
            <div className="space-y-2">
              {section.items.map(item => (
                <Link
                  key={item.path}
                  to={item.path}
                  className={`block p-3 rounded-lg transition-colors ${
                    location.pathname === item.path
                      ? 'bg-white/20 text-white'
                      : 'hover:bg-white/10 text-gray-300'
                  }`}
                >
                  <div className="flex items-center gap-3">
                    <span className="text-xl">{item.icon}</span>
                    <div>
                      <div className="font-medium">{item.label}</div>
                      {item.description && (
                        <div className="text-xs text-gray-400">{item.description}</div>
                      )}
                    </div>
                  </div>
                </Link>
              ))}
            </div>
          </div>
        ))}
      </div>

      {/* Quick Stats */}
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mt-8">
        <div className="bg-gray-800 rounded-lg p-4 text-center">
          <div className="text-2xl font-bold text-blue-400">190</div>
          <div className="text-gray-400 text-sm">Municipalities</div>
        </div>
        <div className="bg-gray-800 rounded-lg p-4 text-center">
          <div className="text-2xl font-bold text-green-400">10,791</div>
          <div className="text-gray-400 text-sm">Entities</div>
        </div>
        <div className="bg-gray-800 rounded-lg p-4 text-center">
          <div className="text-2xl font-bold text-yellow-400">7,458</div>
          <div className="text-gray-400 text-sm">Accommodations</div>
        </div>
        <div className="bg-gray-800 rounded-lg p-4 text-center">
          <div className="text-2xl font-bold text-purple-400">37</div>
          <div className="text-gray-400 text-sm">Staging Properties</div>
        </div>
      </div>
    </div>
  );
}
```

## Step 4: Create Navigation Hub Page

Create client/src/pages/NavigationHub.tsx:
```tsx
import React from 'react';
import MainNav from '../components/MainNav';

export default function NavigationHub() {
  return (
    <div className="min-h-screen bg-gray-900">
      <MainNav />
    </div>
  );
}
```

## Step 5: Add Hub Route

In App.tsx, add:
```tsx
import NavigationHub from './pages/NavigationHub';

// Add route:
<Route path="/hub" element={<NavigationHub />} />
```

## Step 6: Update AdminLayout Sidebar

In client/src/pages/AdminLayout.tsx (or wherever sidebar is defined), update the navigation structure:
```tsx
const sidebarSections = [
  {
    title: 'NAVIGATION',
    items: [
      { path: '/hub', label: 'Navigation Hub', icon: '🏠' },
      { path: '/command-center', label: 'Command Center', icon: '🛡️' },
      { path: '/', label: 'Public Site', icon: '🌐' },
    ]
  },
  {
    title: 'COMMUNITY DATA',
    items: [
      { path: '/admin/geo', label: 'Geographic View', icon: '🗺️' },
      { path: '/admin/infrastructure', label: 'Infrastructure', icon: '🏗️' },
      { path: '/admin/chambers', label: 'Chambers', icon: '🏛️' },
      { path: '/admin/naics', label: 'NAICS Explorer', icon: '📋' },
    ]
  },
  {
    title: 'ACCOMMODATIONS',
    items: [
      { path: '/admin/accommodations', label: 'All Properties', icon: '🏨' },
      { path: '/staging', label: 'Staging Search', icon: '🔍' },
      { path: '/host/dashboard', label: 'Host Portal', icon: '🏠' },
    ]
  },
  {
    title: 'OPERATIONS',
    items: [
      { path: '/admin/civos', label: 'CivOS Dashboard', icon: '📡' },
      { path: '/fleet', label: 'Vehicle Fleet', icon: '🚛' },
      { path: '/admin/import', label: 'Data Import', icon: '📥' },
      { path: '/admin/sources', label: 'Manage Sources', icon: '🔗' },
      { path: '/admin/matrix', label: 'Source Matrix', icon: '📈' },
      { path: '/admin/logs', label: 'Scrape Logs', icon: '📝' },
    ]
  },
  {
    title: 'SYSTEM',
    items: [
      { path: '/admin', label: 'Overview', icon: '📊' },
      { path: '/admin/settings', label: 'Settings', icon: '⚙️' },
      { path: '/admin/docs', label: 'Documentation', icon: '📚' },
    ]
  },
];
```

## Step 7: Verify Command Center Works

After changes, test these URLs:
- `/hub` - New navigation hub
- `/command-center` - Should show Dashboard.tsx with municipality toggle
- `/legacy` - Same as command-center (alias)
- `/fleet` - Should show FleetDashboard component
- `/admin/civos` - CivOS operations

Report which routes work and which need fixes.

Summary: What This Does
ChangePurpose/hubNew central navigation page showing all 4 areas/command-centerProper name for Dashboard.tsx (keeps /legacy as alias)/fleetExposes the existing FleetDashboard componentUpdated AdminLayoutCleaner sidebar with all areas accessibleMainNav componentReusable navigation grid for the hub
Nothing is deleted - we're just adding routes and organizing navigation.
Run this and let me know what works! 