Perfect — here’s a drop-in GitHub Actions summary step that reads your generated cert and prints a clean PASS / HELD / FAIL block directly into the PR check summary.

Add this after the SCM ingest step and before artifact upload (order doesn’t strictly matter, but this reads the cert).

✅ GitHub Actions — SCM Certification Summary Step
      - name: SCM Certification Summary
        if: always()
        run: |
          CERT_PATH="artifacts/qa/scm/p2-operator-cert.json"

          if [ ! -f "$CERT_PATH" ]; then
            echo "### ❌ SCM Certification" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- Certification file not found." >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          OVERALL=$(node -e "const c=require('./$CERT_PATH'); console.log(c.summary.overall_status)")
          PASS=$(node -e "const c=require('./$CERT_PATH'); console.log(c.summary.pass_modules)")
          HELD=$(node -e "const c=require('./$CERT_PATH'); console.log(c.summary.held_modules)")
          FAIL=$(node -e "const c=require('./$CERT_PATH'); console.log(c.summary.fail_modules)")
          SHA=$(node -e "const c=require('./$CERT_PATH'); console.log(c.input.proof_sha256)")

          echo "### 🧾 SCM Certification Result" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Overall Status:** \`$OVERALL\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| PASS modules | $PASS |" >> $GITHUB_STEP_SUMMARY
          echo "| HELD modules | $HELD |" >> $GITHUB_STEP_SUMMARY
          echo "| FAIL modules | $FAIL |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Proof SHA256:** \`$SHA\`" >> $GITHUB_STEP_SUMMARY

          if [ "$OVERALL" = "PASS" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "✅ All required modules certified." >> $GITHUB_STEP_SUMMARY
          elif [ "$OVERALL" = "HELD" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "⚠️ Certification HELD — optional modules incomplete or authority proof incomplete." >> $GITHUB_STEP_SUMMARY
          else
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "❌ Certification FAILED — required module proof missing or failed." >> $GITHUB_STEP_SUMMARY
          fi

What this gives you in every PR

A clear, human-readable certification verdict at the top of the check

Explicit distinction between:

✅ PASS (fully certifiable)

⚠️ HELD (no guessing, incomplete proof)

❌ FAIL (hard break)

A cryptographic link (SHA256) tying the verdict to the proof bundle

No need to open artifacts just to know if the system is safe

This completes the loop:

Code → Proof → Certification → Human-readable trust signal