Create the main dashboard layout with a responsive grid design.

Create file: client/src/components/Dashboard/DashboardLayout.tsx

import React, { useState, useEffect } from 'react';

interface DashboardLayoutProps {
  defaultRegion?: string;
}

export function DashboardLayout({ defaultRegion = 'bc' }: DashboardLayoutProps) {
  const [selectedRegion, setSelectedRegion] = useState(defaultRegion);
  const [activeTab, setActiveTab] = useState<'overview' | 'map' | 'webcams' | 'alerts'>('overview');
  const [alertCount, setAlertCount] = useState(0);

  // Fetch alert count
  useEffect(() => {
    fetch('/api/v1/alerts/count')
      .then(r => r.json())
      .then(data => setAlertCount(data.count || 0))
      .catch(() => setAlertCount(0));
  }, []);

  return (
    <div className="min-h-screen bg-gray-50">
      {/* Header */}
      <header className="bg-white border-b border-gray-200 sticky top-0 z-50">
        <div className="max-w-7xl mx-auto px-4 py-3">
          <div className="flex items-center justify-between">
            <div className="flex items-center gap-3">
              <span className="text-2xl">🍁</span>
              <h1 className="text-xl font-bold text-gray-900">BC Community Status</h1>
              <span className="text-xs bg-blue-100 text-blue-700 px-2 py-0.5 rounded-full font-medium">
                LIVE
              </span>
            </div>
            
            <div className="flex items-center gap-4">
              {/* Region Selector Placeholder */}
              <select 
                value={selectedRegion}
                onChange={(e) => setSelectedRegion(e.target.value)}
                className="px-3 py-2 border border-gray-300 rounded-lg text-sm"
              >
                <option value="bc">🍁 All British Columbia</option>
                <option value="metro-vancouver">Metro Vancouver</option>
                <option value="capital">Capital Region</option>
                <option value="fraser-valley">Fraser Valley</option>
              </select>
              
              {/* Notification Bell */}
              <button className="relative p-2 text-gray-500 hover:text-gray-700">
                <span className="text-xl">🔔</span>
                {alertCount > 0 && (
                  <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center">
                    {alertCount}
                  </span>
                )}
              </button>
            </div>
          </div>
          
          {/* Tab Navigation */}
          <nav className="flex gap-1 mt-3 -mb-px">
            {[
              { id: 'overview', label: 'Overview', icon: '📊' },
              { id: 'map', label: 'Map', icon: '🗺️' },
              { id: 'webcams', label: 'Webcams', icon: '📷' },
              { id: 'alerts', label: 'Alerts', icon: '🚨' },
            ].map(tab => (
              <button
                key={tab.id}
                onClick={() => setActiveTab(tab.id as any)}
                className={`px-4 py-2 text-sm font-medium rounded-t-lg border-b-2 transition-colors ${
                  activeTab === tab.id
                    ? 'border-blue-500 text-blue-600 bg-blue-50'
                    : 'border-transparent text-gray-500 hover:text-gray-700 hover:bg-gray-50'
                }`}
              >
                <span className="mr-1.5">{tab.icon}</span>
                {tab.label}
              </button>
            ))}
          </nav>
        </div>
      </header>

      {/* Main Content */}
      <main className="max-w-7xl mx-auto px-4 py-6">
        {activeTab === 'overview' && (
          <OverviewPlaceholder regionId={selectedRegion} />
        )}
        {activeTab === 'map' && (
          <div className="bg-white rounded-xl shadow-sm p-8 text-center">
            <span className="text-4xl">🗺️</span>
            <p className="text-gray-600 mt-2">Map view coming soon</p>
          </div>
        )}
        {activeTab === 'webcams' && (
          <div className="bg-white rounded-xl shadow-sm p-8 text-center">
            <span className="text-4xl">📷</span>
            <p className="text-gray-600 mt-2">Webcams tab - 1,100 cameras</p>
          </div>
        )}
        {activeTab === 'alerts' && (
          <div className="bg-white rounded-xl shadow-sm p-8 text-center">
            <span className="text-4xl">🚨</span>
            <p className="text-gray-600 mt-2">Alerts tab coming soon</p>
          </div>
        )}
      </main>
      
      {/* Footer */}
      <footer className="bg-white border-t border-gray-200 py-4 mt-8">
        <div className="max-w-7xl mx-auto px-4 text-center text-sm text-gray-500">
          <p>Data refreshes automatically • <LiveTimestamp /></p>
          <p className="mt-1">10,330 entities • 190 regions • 10 live data sources</p>
        </div>
      </footer>
    </div>
  );
}

// Temporary placeholder for Overview tab
function OverviewPlaceholder({ regionId }: { regionId: string }) {
  return (
    <div className="space-y-6">
      {/* Status Cards Placeholder */}
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
        <StatusCardPlaceholder icon="🚨" title="Alerts" value="3" color="yellow" />
        <StatusCardPlaceholder icon="⛴️" title="Ferries" value="On Time" color="green" />
        <StatusCardPlaceholder icon="🌤️" title="Weather" value="-2°C" color="blue" />
        <StatusCardPlaceholder icon="🛣️" title="Roads" value="2 Events" color="gray" />
      </div>
      
      {/* Content Grid Placeholder */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <div className="lg:col-span-2 bg-white rounded-xl shadow-sm p-6">
          <h3 className="font-semibold text-gray-900 mb-4">🚨 Live Alerts</h3>
          <p className="text-gray-500">Alert feed will appear here</p>
        </div>
        <div className="bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl p-6 text-white">
          <h3 className="font-semibold mb-2">🌤️ Weather</h3>
          <p className="text-4xl font-bold">-2°C</p>
          <p className="opacity-80">Light Snow</p>
        </div>
      </div>
      
      {/* Webcam Grid Placeholder */}
      <div className="bg-white rounded-xl shadow-sm p-6">
        <h3 className="font-semibold text-gray-900 mb-4">📷 Live Webcams</h3>
        <div className="grid grid-cols-4 gap-4">
          {[1,2,3,4].map(i => (
            <div key={i} className="aspect-video bg-gray-200 rounded-lg animate-pulse" />
          ))}
        </div>
      </div>
    </div>
  );
}

function StatusCardPlaceholder({ icon, title, value, color }: { 
  icon: string; title: string; value: string; color: string 
}) {
  const colorClasses: Record<string, string> = {
    red: 'bg-red-50 border-red-200',
    yellow: 'bg-yellow-50 border-yellow-200',
    green: 'bg-green-50 border-green-200',
    blue: 'bg-blue-50 border-blue-200',
    gray: 'bg-gray-50 border-gray-200',
  };
  
  return (
    <div className={`rounded-xl p-4 border-2 ${colorClasses[color]}`}>
      <div className="flex items-center justify-between mb-2">
        <span className="text-2xl">{icon}</span>
        <span className="text-xs font-medium uppercase tracking-wide text-gray-500">{title}</span>
      </div>
      <div className="text-2xl font-bold text-gray-900">{value}</div>
    </div>
  );
}

function LiveTimestamp() {
  const [time, setTime] = useState(new Date());
  
  useEffect(() => {
    const interval = setInterval(() => setTime(new Date()), 1000);
    return () => clearInterval(interval);
  }, []);
  
  return <span>Last updated: {time.toLocaleTimeString()}</span>;
}

export default DashboardLayout;