Create a Service Catalog browser page that shows categories, services, and allows exploring the service offerings.

## Create file: client/src/pages/services/ServiceCatalog.tsx
```tsx
import React, { useState, useEffect, useCallback } from 'react';
import { useAuth } from '../../contexts/AuthContext';

interface Category {
  id: string;
  name: string;
  slug: string;
  description: string;
  icon: string;
  serviceCount: number;
}

interface Service {
  id: string;
  name: string;
  slug: string;
  description: string;
  typicalDurationMinHours: number;
  typicalDurationMaxHours: number;
  crewMin: number;
  crewTypical: number;
  crewMax: number;
  canBeEmergency: boolean;
  weatherDependent: boolean;
  revisitCycle: string;
  category: {
    name: string;
    slug: string;
    icon: string;
  };
}

interface ServiceDetail {
  id: string;
  name: string;
  slug: string;
  description: string;
  typicalDurationMinHours: number;
  typicalDurationMaxHours: number;
  crewMin: number;
  crewTypical: number;
  crewMax: number;
  noise: string;
  disruption: string;
  failureRiskIfDelayed: string;
  canBeEmergency: boolean;
  requiresOwnerPresent: boolean;
  canBeDoneVacant: boolean;
  weatherDependent: boolean;
  revisitCycle: string;
  category: {
    name: string;
    slug: string;
    icon: string;
  };
  seasonality: Array<{
    climateRegionName: string;
    earliestWeek: number;
    latestWeek: number;
    hardStop: boolean;
    rainSensitive: boolean;
    snowSensitive: boolean;
    windSensitive: boolean;
  }>;
  pricing: {
    pricingModel: string;
    basePrice: number;
    unitDescriptor: string;
    remoteMultiplier: number;
    minimumCharge: number;
  } | null;
  certifications: Array<{
    name: string;
    authority: string;
    isRequired: boolean;
  }>;
  accessRequirements: Array<{
    name: string;
    description: string;
  }>;
  mobilizationClass: {
    name: string;
    baseCost: number;
  } | null;
}

interface Stats {
  categories: number;
  services: number;
  bundles: number;
  communities: number;
  climateRegions: number;
  currentWeek: number;
}

export default function ServiceCatalog() {
  const { token } = useAuth();
  const [categories, setCategories] = useState<Category[]>([]);
  const [services, setServices] = useState<Service[]>([]);
  const [selectedCategory, setSelectedCategory] = useState<string | null>(null);
  const [selectedService, setSelectedService] = useState<ServiceDetail | null>(null);
  const [stats, setStats] = useState<Stats | null>(null);
  const [search, setSearch] = useState('');
  const [loading, setLoading] = useState(true);
  const [detailLoading, setDetailLoading] = useState(false);
  const [viewMode, setViewMode] = useState<'grid' | 'list'>('grid');

  // Load initial data
  useEffect(() => {
    async function loadData() {
      setLoading(true);
      try {
        const [statsRes, categoriesRes] = await Promise.all([
          fetch('/api/service-runs/stats'),
          fetch('/api/service-runs/categories')
        ]);
        
        const statsData = await statsRes.json();
        const categoriesData = await categoriesRes.json();
        
        if (statsData.success) setStats(statsData.stats);
        if (categoriesData.success) setCategories(categoriesData.categories);
      } catch (err) {
        console.error('Failed to load catalog:', err);
      } finally {
        setLoading(false);
      }
    }
    loadData();
  }, []);

  // Load services when category changes or search
  const loadServices = useCallback(async () => {
    try {
      let url = '/api/service-runs/services?';
      if (selectedCategory) url += `category=${selectedCategory}&`;
      if (search) url += `search=${encodeURIComponent(search)}`;
      
      const res = await fetch(url);
      const data = await res.json();
      
      if (data.success) {
        setServices(data.services);
      }
    } catch (err) {
      console.error('Failed to load services:', err);
    }
  }, [selectedCategory, search]);

  useEffect(() => {
    loadServices();
  }, [loadServices]);

  // Load service details
  async function loadServiceDetail(slug: string) {
    setDetailLoading(true);
    try {
      const res = await fetch(`/api/service-runs/services/${slug}`);
      const data = await res.json();
      
      if (data.success) {
        setSelectedService(data.service);
      }
    } catch (err) {
      console.error('Failed to load service detail:', err);
    } finally {
      setDetailLoading(false);
    }
  }

  const revisitCycleLabels: Record<string, string> = {
    annual: 'Annually',
    biannual: 'Twice a year',
    quarterly: 'Quarterly',
    monthly: 'Monthly',
    weekly: 'Weekly',
    ad_hoc: 'As needed'
  };

  const riskColors: Record<string, string> = {
    low: 'text-green-400',
    medium: 'text-yellow-400',
    high: 'text-red-400'
  };

  if (loading) {
    return (
      <div className="p-6 text-center text-gray-400">
        Loading service catalog...
      </div>
    );
  }

  return (
    <div className="p-6">
      {/* Header */}
      <div className="flex items-center justify-between mb-6">
        <div>
          <h1 className="text-2xl font-bold text-white">Service Catalog</h1>
          <p className="text-gray-400">Browse {stats?.services} services across {stats?.categories} categories</p>
        </div>
        <div className="flex items-center gap-4">
          <div className="bg-gray-800 rounded-lg px-4 py-2 text-center">
            <div className="text-lg font-bold text-blue-400">Week {stats?.currentWeek}</div>
            <div className="text-xs text-gray-500">Current</div>
          </div>
        </div>
      </div>

      {/* Stats Bar */}
      <div className="grid grid-cols-5 gap-4 mb-6">
        <div className="bg-gray-800 rounded-lg p-4 text-center">
          <div className="text-2xl font-bold text-white">{stats?.categories}</div>
          <div className="text-sm text-gray-400">Categories</div>
        </div>
        <div className="bg-gray-800 rounded-lg p-4 text-center">
          <div className="text-2xl font-bold text-white">{stats?.services}</div>
          <div className="text-sm text-gray-400">Services</div>
        </div>
        <div className="bg-gray-800 rounded-lg p-4 text-center">
          <div className="text-2xl font-bold text-white">{stats?.bundles}</div>
          <div className="text-sm text-gray-400">Bundles</div>
        </div>
        <div className="bg-gray-800 rounded-lg p-4 text-center">
          <div className="text-2xl font-bold text-white">{stats?.climateRegions}</div>
          <div className="text-sm text-gray-400">Climate Zones</div>
        </div>
        <div className="bg-gray-800 rounded-lg p-4 text-center">
          <div className="text-2xl font-bold text-white">{stats?.communities}</div>
          <div className="text-sm text-gray-400">Communities</div>
        </div>
      </div>

      {/* Search and Filters */}
      <div className="bg-gray-800 rounded-lg p-4 mb-6">
        <div className="flex flex-wrap gap-4 items-center">
          <div className="flex-1 min-w-[250px]">
            <input
              type="text"
              placeholder="Search services..."
              value={search}
              onChange={(e) => setSearch(e.target.value)}
              className="w-full bg-gray-700 border border-gray-600 rounded-lg px-4 py-2 text-white placeholder-gray-400"
            />
          </div>
          <select
            value={selectedCategory || ''}
            onChange={(e) => setSelectedCategory(e.target.value || null)}
            className="bg-gray-700 border border-gray-600 rounded-lg px-4 py-2 text-white"
          >
            <option value="">All Categories</option>
            {categories.map(cat => (
              <option key={cat.id} value={cat.slug}>
                {cat.icon} {cat.name} ({cat.serviceCount})
              </option>
            ))}
          </select>
          <div className="flex gap-1 bg-gray-700 rounded-lg p-1">
            <button
              onClick={() => setViewMode('grid')}
              className={`px-3 py-1 rounded ${viewMode === 'grid' ? 'bg-blue-600 text-white' : 'text-gray-400'}`}
            >
              Grid
            </button>
            <button
              onClick={() => setViewMode('list')}
              className={`px-3 py-1 rounded ${viewMode === 'list' ? 'bg-blue-600 text-white' : 'text-gray-400'}`}
            >
              List
            </button>
          </div>
        </div>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {/* Categories Sidebar (when no category selected) */}
        {!selectedCategory && !search && (
          <div className="lg:col-span-3">
            <h2 className="text-lg font-semibold text-white mb-4">Browse by Category</h2>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
              {categories.map(cat => (
                <button
                  key={cat.id}
                  onClick={() => setSelectedCategory(cat.slug)}
                  className="bg-gray-800 rounded-lg p-4 text-left hover:bg-gray-750 transition-colors border border-gray-700 hover:border-blue-500"
                >
                  <div className="text-3xl mb-2">{cat.icon}</div>
                  <div className="font-medium text-white">{cat.name}</div>
                  <div className="text-sm text-gray-400">{cat.serviceCount} services</div>
                </button>
              ))}
            </div>
          </div>
        )}

        {/* Services List */}
        {(selectedCategory || search) && (
          <div className="lg:col-span-2">
            <div className="flex items-center justify-between mb-4">
              <div className="flex items-center gap-2">
                {selectedCategory && (
                  <button
                    onClick={() => setSelectedCategory(null)}
                    className="text-blue-400 hover:text-blue-300"
                  >
                    ← All Categories
                  </button>
                )}
                <h2 className="text-lg font-semibold text-white">
                  {selectedCategory 
                    ? categories.find(c => c.slug === selectedCategory)?.name 
                    : `Search Results`
                  } 
                  <span className="text-gray-400 font-normal ml-2">({services.length})</span>
                </h2>
              </div>
            </div>

            {viewMode === 'grid' ? (
              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                {services.map(service => (
                  <div
                    key={service.id}
                    onClick={() => loadServiceDetail(service.slug)}
                    className={`bg-gray-800 rounded-lg p-4 cursor-pointer hover:bg-gray-750 transition-colors border ${
                      selectedService?.slug === service.slug 
                        ? 'border-blue-500' 
                        : 'border-gray-700 hover:border-gray-600'
                    }`}
                  >
                    <div className="flex items-start justify-between mb-2">
                      <div className="flex items-center gap-2">
                        <span className="text-xl">{service.category.icon}</span>
                        <h3 className="font-medium text-white">{service.name}</h3>
                      </div>
                      {service.canBeEmergency && (
                        <span className="text-xs bg-red-500/20 text-red-400 px-2 py-0.5 rounded">
                          🚨 Emergency
                        </span>
                      )}
                    </div>
                    <p className="text-sm text-gray-400 mb-3 line-clamp-2">{service.description}</p>
                    <div className="flex flex-wrap gap-2 text-xs">
                      <span className="bg-gray-700 px-2 py-1 rounded text-gray-300">
                        ⏱️ {service.typicalDurationMinHours}-{service.typicalDurationMaxHours}h
                      </span>
                      <span className="bg-gray-700 px-2 py-1 rounded text-gray-300">
                        👥 {service.crewTypical} crew
                      </span>
                      {service.weatherDependent && (
                        <span className="bg-yellow-500/20 px-2 py-1 rounded text-yellow-400">
                          🌤️ Weather
                        </span>
                      )}
                      {service.revisitCycle && service.revisitCycle !== 'ad_hoc' && (
                        <span className="bg-blue-500/20 px-2 py-1 rounded text-blue-400">
                          🔄 {revisitCycleLabels[service.revisitCycle]}
                        </span>
                      )}
                    </div>
                  </div>
                ))}
              </div>
            ) : (
              <div className="bg-gray-800 rounded-lg overflow-hidden">
                <table className="w-full">
                  <thead className="bg-gray-700">
                    <tr>
                      <th className="text-left px-4 py-3 text-gray-300 font-medium">Service</th>
                      <th className="text-left px-4 py-3 text-gray-300 font-medium">Duration</th>
                      <th className="text-left px-4 py-3 text-gray-300 font-medium">Crew</th>
                      <th className="text-left px-4 py-3 text-gray-300 font-medium">Cycle</th>
                    </tr>
                  </thead>
                  <tbody className="divide-y divide-gray-700">
                    {services.map(service => (
                      <tr 
                        key={service.id}
                        onClick={() => loadServiceDetail(service.slug)}
                        className={`cursor-pointer hover:bg-gray-750 ${
                          selectedService?.slug === service.slug ? 'bg-gray-700' : ''
                        }`}
                      >
                        <td className="px-4 py-3">
                          <div className="flex items-center gap-2">
                            <span>{service.category.icon}</span>
                            <div>
                              <div className="text-white font-medium">{service.name}</div>
                              <div className="text-gray-500 text-xs truncate max-w-xs">{service.description}</div>
                            </div>
                          </div>
                        </td>
                        <td className="px-4 py-3 text-gray-300">
                          {service.typicalDurationMinHours}-{service.typicalDurationMaxHours}h
                        </td>
                        <td className="px-4 py-3 text-gray-300">{service.crewTypical}</td>
                        <td className="px-4 py-3 text-gray-400 text-sm">
                          {revisitCycleLabels[service.revisitCycle] || '-'}
                        </td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            )}
          </div>
        )}

        {/* Service Detail Panel */}
        {(selectedCategory || search) && (
          <div className="lg:col-span-1">
            {selectedService ? (
              <div className="bg-gray-800 rounded-lg p-4 sticky top-4">
                {detailLoading ? (
                  <div className="text-center py-8 text-gray-500">Loading...</div>
                ) : (
                  <>
                    {/* Header */}
                    <div className="pb-4 border-b border-gray-700 mb-4">
                      <div className="flex items-center gap-2 mb-2">
                        <span className="text-2xl">{selectedService.category.icon}</span>
                        <h3 className="text-xl font-bold text-white">{selectedService.name}</h3>
                      </div>
                      <p className="text-gray-400 text-sm">{selectedService.description}</p>
                      
                      <div className="flex flex-wrap gap-2 mt-3">
                        {selectedService.canBeEmergency && (
                          <span className="text-xs bg-red-500/20 text-red-400 px-2 py-1 rounded border border-red-500/50">
                            🚨 Emergency Available
                          </span>
                        )}
                        {selectedService.weatherDependent && (
                          <span className="text-xs bg-yellow-500/20 text-yellow-400 px-2 py-1 rounded border border-yellow-500/50">
                            🌤️ Weather Dependent
                          </span>
                        )}
                      </div>
                    </div>

                    {/* Quick Stats */}
                    <div className="grid grid-cols-3 gap-2 mb-4">
                      <div className="bg-gray-700 rounded p-2 text-center">
                        <div className="text-lg font-bold text-white">
                          {selectedService.typicalDurationMinHours}-{selectedService.typicalDurationMaxHours}
                        </div>
                        <div className="text-xs text-gray-400">Hours</div>
                      </div>
                      <div className="bg-gray-700 rounded p-2 text-center">
                        <div className="text-lg font-bold text-white">
                          {selectedService.crewMin}-{selectedService.crewMax}
                        </div>
                        <div className="text-xs text-gray-400">Crew Size</div>
                      </div>
                      <div className="bg-gray-700 rounded p-2 text-center">
                        <div className={`text-lg font-bold ${riskColors[selectedService.failureRiskIfDelayed]}`}>
                          {selectedService.failureRiskIfDelayed.toUpperCase()}
                        </div>
                        <div className="text-xs text-gray-400">Risk</div>
                      </div>
                    </div>

                    {/* Pricing */}
                    {selectedService.pricing && (
                      <div className="mb-4">
                        <h4 className="text-sm font-medium text-gray-300 mb-2">💰 Pricing</h4>
                        <div className="bg-gray-700 rounded p-3">
                          <div className="text-2xl font-bold text-green-400">
                            ${selectedService.pricing.basePrice.toFixed(2)}
                          </div>
                          <div className="text-sm text-gray-400">
                            {selectedService.pricing.unitDescriptor || selectedService.pricing.pricingModel}
                          </div>
                          {selectedService.pricing.minimumCharge > 0 && (
                            <div className="text-xs text-gray-500 mt-1">
                              Minimum: ${selectedService.pricing.minimumCharge.toFixed(2)}
                            </div>
                          )}
                        </div>
                      </div>
                    )}

                    {/* Seasonality */}
                    {selectedService.seasonality.length > 0 && (
                      <div className="mb-4">
                        <h4 className="text-sm font-medium text-gray-300 mb-2">📅 Seasonality</h4>
                        <div className="space-y-2">
                          {selectedService.seasonality.map((s, i) => (
                            <div key={i} className="bg-gray-700 rounded p-2 text-sm">
                              <div className="flex items-center justify-between">
                                <span className="text-white">{s.climateRegionName}</span>
                                <span className="text-gray-400">
                                  Weeks {s.earliestWeek}-{s.latestWeek}
                                </span>
                              </div>
                              {(s.rainSensitive || s.snowSensitive || s.windSensitive) && (
                                <div className="flex gap-2 mt-1">
                                  {s.rainSensitive && <span className="text-xs text-blue-400">🌧️ Rain</span>}
                                  {s.snowSensitive && <span className="text-xs text-blue-300">❄️ Snow</span>}
                                  {s.windSensitive && <span className="text-xs text-gray-400">💨 Wind</span>}
                                </div>
                              )}
                            </div>
                          ))}
                        </div>
                      </div>
                    )}

                    {/* Certifications */}
                    {selectedService.certifications.length > 0 && (
                      <div className="mb-4">
                        <h4 className="text-sm font-medium text-gray-300 mb-2">📜 Required Certifications</h4>
                        <div className="space-y-1">
                          {selectedService.certifications.map((c, i) => (
                            <div key={i} className="flex items-center justify-between text-sm">
                              <span className="text-gray-300">{c.name}</span>
                              <span className="text-gray-500 text-xs">{c.authority}</span>
                            </div>
                          ))}
                        </div>
                      </div>
                    )}

                    {/* Mobilization */}
                    {selectedService.mobilizationClass && (
                      <div className="mb-4">
                        <h4 className="text-sm font-medium text-gray-300 mb-2">🚛 Mobilization</h4>
                        <div className="bg-gray-700 rounded p-2 text-sm">
                          <div className="flex items-center justify-between">
                            <span className="text-gray-300 capitalize">
                              {selectedService.mobilizationClass.name.replace(/_/g, ' ')}
                            </span>
                            <span className="text-gray-400">
                              ${selectedService.mobilizationClass.baseCost.toFixed(2)}
                            </span>
                          </div>
                        </div>
                      </div>
                    )}

                    {/* Operational Details */}
                    <div className="mb-4">
                      <h4 className="text-sm font-medium text-gray-300 mb-2">⚙️ Operations</h4>
                      <div className="grid grid-cols-2 gap-2 text-xs">
                        <div className="bg-gray-700 rounded p-2">
                          <span className="text-gray-400">Noise Level: </span>
                          <span className="text-white capitalize">{selectedService.noise}</span>
                        </div>
                        <div className="bg-gray-700 rounded p-2">
                          <span className="text-gray-400">Disruption: </span>
                          <span className="text-white capitalize">{selectedService.disruption}</span>
                        </div>
                        <div className="bg-gray-700 rounded p-2">
                          <span className="text-gray-400">Owner Present: </span>
                          <span className="text-white">{selectedService.requiresOwnerPresent ? 'Yes' : 'No'}</span>
                        </div>
                        <div className="bg-gray-700 rounded p-2">
                          <span className="text-gray-400">Vacant OK: </span>
                          <span className="text-white">{selectedService.canBeDoneVacant ? 'Yes' : 'No'}</span>
                        </div>
                      </div>
                    </div>

                    {/* Actions */}
                    <div className="pt-4 border-t border-gray-700 space-y-2">
                      <button className="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded text-sm">
                        Add to Service Run
                      </button>
                      <button className="w-full bg-gray-700 hover:bg-gray-600 text-white py-2 rounded text-sm">
                        Find Compatible Services
                      </button>
                    </div>
                  </>
                )}
              </div>
            ) : (
              <div className="bg-gray-800 rounded-lg p-8 text-center text-gray-500 sticky top-4">
                <div className="text-4xl mb-3">🔧</div>
                <p>Select a service to view details</p>
              </div>
            )}
          </div>
        )}
      </div>
    </div>
  );
}
```

## Add route to App.tsx:
```tsx
import ServiceCatalog from './pages/services/ServiceCatalog';

// Add route:
<Route path="/services" element={<ServiceCatalog />} />
<Route path="/services/catalog" element={<ServiceCatalog />} />
```

## Add to navigation sidebar:

In AdminLayout or your navigation, add a new section:
```tsx
{
  title: '🔧 Services',
  defaultOpen: true,
  items: [
    { path: '/services/catalog', label: 'Service Catalog', icon: '📚' },
  ]
}
```

## Test the page at /services/catalog

It should show:
- Stats bar (159 services, 16 categories, etc.)
- Category cards to browse
- Service list with grid/list toggle
- Service detail panel with pricing, seasonality, certifications