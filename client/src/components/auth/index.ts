/**
 * Auth UI Components
 * 
 * PROMPT-5: Client-side visibility gating components.
 * These are VISIBILITY-ONLY - backend always enforces via PROMPT-3/4.
 */

export { NotAuthorized } from './NotAuthorized';
export { RequireCapability } from './RequireCapability';
export { GatedButton } from './GatedButton';
